!(function () { function e (e) { for (let t = 0, n = 1; ;t += n, n /= 2) if (e >= (7 - 4 * t) / 11) return -Math.pow((11 - 6 * t - 11 * e) / 4, 2) + Math.pow(n, 2) } function t (e) { return function (t) { return t < 0.5 ? e(2 * t) / 2 : (2 - e(2 * (1 - t))) / 2 } } function n (e) { return e } function r (e) { return Math.pow(e, 2) } function u (e) { return Math.pow(e, 3) } const o = o => { switch (!0) { case o % 2 == 0:return e; case o % 3 == 0:return n; case o % 4 == 0:return t(n); case o % 5 == 0:return t(e); case o % 6 == 0:return u; default:return r } }; document.addEventListener('DOMContentLoaded', function () { [...document.querySelector('.cv').querySelectorAll('p')].forEach((e, t) => { let n = e.outerHTML; let r = n.length; regex = /<(\/)??p>/gi, n = n.replace(regex, ''), (function (e) { let t = performance.now(); requestAnimationFrame(function n (r) { let u = (r - t) / e.duration; let o = e.timing(u); e.draw(o), u < 1 && requestAnimationFrame(n) }) }({ duration: 1e4, timing: o(t), draw: t => { let u = (r - 0) * t + 0; e.innerHTML = n.substr(0, Math.ceil(u)) } })) }) }, !0) }())
